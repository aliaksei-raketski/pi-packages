{
  "$schema": "https://json-schema.org/schema",
  "$id": "PiSkill",
  "title": "Create a Pi skill",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Skill name. Use letters, numbers, spaces, underscores, or hyphens; it is normalized to lowercase hyphen form for Agent Skills compatibility.",
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9 _-]*[A-Za-z0-9])?$",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What skill would you like to create?"
    },
    "project": {
      "type": "string",
      "description": "Target Pi package project.",
      "$default": {
        "$source": "projectName"
      },
      "x-dropdown": "projects",
      "x-prompt": "Which Pi package project should receive this skill?"
    },
    "description": {
      "type": "string",
      "description": "Skill description written to SKILL.md frontmatter."
    },
    "overwrite": {
      "type": "boolean",
      "description": "Overwrite existing skill files.",
      "default": false
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files.",
      "default": false,
      "x-priority": "internal"
    }
  },
  "required": ["name", "project"]
}
