{
  "$schema": "https://json-schema.org/schema",
  "$id": "PiTheme",
  "title": "Create a Pi theme",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Theme name and JSON file name without .json. Use letters, numbers, spaces, underscores, or hyphens; it is normalized to lowercase hyphen form.",
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9 _-]*[A-Za-z0-9])?$",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What theme would you like to create?"
    },
    "project": {
      "type": "string",
      "description": "Target Pi package project.",
      "$default": {
        "$source": "projectName"
      },
      "x-dropdown": "projects",
      "x-prompt": "Which Pi package project should receive this theme?"
    },
    "overwrite": {
      "type": "boolean",
      "description": "Overwrite existing theme files.",
      "default": false
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files.",
      "default": false,
      "x-priority": "internal"
    }
  },
  "required": ["name", "project"]
}
